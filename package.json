{
  "name": "vsn-ganesh-new1",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "postinstall": "npm clean-install --prefix website --progress=false",
    "dev": "npm run dev --prefix website",
    "build": "npm run build --prefix website",
    "typecheck": "npm run typecheck --prefix website",
    "lint": "npm run lint --prefix website",
    "test": "npm test --prefix website",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
